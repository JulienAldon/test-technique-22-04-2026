info:
  name: GET reviews
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/reviews
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
