info:
  name: GET movies
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/movies
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
