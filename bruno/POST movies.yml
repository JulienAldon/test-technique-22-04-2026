info:
  name: POST movies
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/movies/
  body:
    type: json
    data: |-
      {
        "title": "harry potter 1",
        "description": "film super",
        "actor_ids": [1]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
